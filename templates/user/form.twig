{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block index %}active{% endblock %}

{% block body %}

    <main role="main">
        <div class="container mt-3">
            <div class="row justify-content-center">
                <div class="col-lg-7">
                    <div class="text-success text-center">{{ message }}</div>
                    <div class="text-danger text-center">{{ error }}</div>
                    <form action="userAdvance" method="post">
                        <div class="form-group">
                            <label class="small mb-1" for="inputEmail">Email</label>
                            <input class="form-control py-4" id="inputEmail" name="inputEmail" type="email"
                                   aria-describedby="emailHelp" placeholder="Votre email" value="{{ user.email }}"/>
                        </div>
                        <div class="form-group">
                            <label class="small mb-1" for="selectRole">Rôle</label>
                            <select class="form-control" id="selectRole" name="selectRole">
                                {% for role in listRoles %}
                                    <option value="{{ role }}"
                                            {% if user.role == role %}selected{% endif %}>
                                        {{ role|capitalize }}
                                    </option>
                                {% endfor %}
                            </select>
                        </div>
                        <div class="form-row">
                            <div class="col-md-12">
                                <div class="form-group">
                                    <label class="small mb-1" for="newPassword">Mot de passe</label>
                                    <input class="form-control py-4" id="newPassword" name="newPassword" type="password"
                                           placeholder="Le nouveau mot de passe"/>
                                </div>
                            </div>
                        </div>
                        <button type="submit" class="btn btn-primary">Valider</button>
                    </form>
                </div>
            </div>
        </div>
    </main>

{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}